const Service = appRequire( 'lib/glass/service' ); // eslint-disable-line


describe( 'Glass Service', () => {

	before( async ( ) => {


	} );

	describe( '#createGlass', () => {
		context( 'Creating Glass', () => {
			it( 'No test written', () => {

				expect( true ).to.equal( true );

			} );
		} );
	} );

	describe( '#updateGlass', () => {
		context( 'Updating Glass', () => {
			it( 'No test written', () => {

				expect( true ).to.equal( true );

			} );
		} );
	} );

	describe( '#deleteGlass', () => {
		context( 'Deleting Glass', () => {
			it( 'No test written', () => {

				expect( true ).to.equal( true );

			} );
		} );
	} );

	after( ( ) => {


	} );

} );
